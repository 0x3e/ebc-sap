const assert = chai.assert
const eq = chai.assert.equal
const dEq = chai.assert.deepEqual
const ok = chai.assert.isOk
const describe = window.describe
const it = window.it
export {describe, it, assert, eq, dEq, ok}
